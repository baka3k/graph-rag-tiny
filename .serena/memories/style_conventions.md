# Style and conventions
- No explicit formatter/linter settings found in repo.
- Python scripts appear standalone; use clear CLI args and dotenv-based config.
- Keep ASCII by default; follow existing naming (snake_case, simple script entrypoints).